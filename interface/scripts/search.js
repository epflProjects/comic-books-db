function postSearch(event) {
    var target = $(event.currentTarget);
    var val = target.attr('data-value');
    var input = target.find('input');
    var index;

    // TODO https://codepen.io/bseth99/pen/fboKH?editors=0010#0
}